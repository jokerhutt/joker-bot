FROM python:3.12-slim

WORKDIR /app

# Install deps
COPY pyproject.toml uv.lock ./
RUN pip install uv && uv pip install --system -r pyproject.toml

# Copy bot code
COPY joker_bot joker_bot

# No EXPOSE (Discord bot, outbound only)

CMD ["python", "-m", "joker_bot.bot.main"]
